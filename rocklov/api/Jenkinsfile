pipeline {
    agent {
        docker {
            image 'ruby'
        }
    }

    stages {
        stage('Prep') {
            steps {
                sh 'cd rocklov/api && bundle install'
            }
        }

        stage('Testing') {
            steps {
                sh 'rspec'
            }
        }
    }
}