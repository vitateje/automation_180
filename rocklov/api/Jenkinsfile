pipeline {
    agent {
        docker {
            image 'ruby'
        }
    }

    stages {
        stage('Prep') {
            steps {
                sh 'bgem install rspec'
            }
        }

        stage('Testing') {
            steps {
                sh 'rspec'
            }
        }
    }
}